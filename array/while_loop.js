// Runs its code as long as its boolean expression is true

// executes 5 times, prints numbers 1 to 5 in ascending order

var number = 1;
while(number <= 5) {
  console.log(number);
  number++;
}

// initialize first train, but only 8 trains running, until 9 -> false so loop stops
var trainNumber = 1;
while(trainNumber <=8{
  console.log("Train #" + trainNumber + " is running");
  trainNumber++;
})

// Using variables instead of values
var trainsOperational = 8;
var trainNumber = 1;

while (trainNumber <= trainsOperational) {
  console.log("Train #" + trainNumber + " is running");
  trainNumber++;
}
