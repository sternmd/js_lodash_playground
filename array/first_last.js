// Get the first element of array.

array = [1, 2, 3];

_.first(array)
// → 1

_.head([1, 2, 3]);
// → 1

_.head([]);
// → undefined

// Get the last element of array.
_.last([1, 2, 3]);
// → 3

_.last(array);
// → 3
